apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "schema-test.fullname" . }}-test-connection"
  labels:
    {{- include "schema-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "schema-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
